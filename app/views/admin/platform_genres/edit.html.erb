<h1>プラットホームジャンル編集</h1>

<%= form_with model: @platform_genre, url: admin_platform_genre_path,method: :patch do |f| %>
  <td><%= f.label "ジャンル名" %></td>
  <td><%= f.text_field :name %></td>
  <td><%= f.submit "変更を保存" %></td>
<% end %>